# 🌲 Treeks - 版本更新日志 (Changelog)

---

## 🚀 [v1.2.1] - 2026-08-03 (画笔与文件强化)

### ✨ 新特性 (New Features)

* **📐 编辑器顶部/底部可收起**: 编辑器顶部可一键收起标题/属性/工具栏（保留返回键与紧凑标题显示），底部可一键收起状态栏与操作区，收起状态自动记忆；移动端书写空间最大化，横屏小屏尤其受用。
* **📤 文件上传实时进度条**: "我的文件"批量上传显示真实进度（当前文件字节进度 + 文件序号），超 50MB 文件自动跳过并提示，完成后显示汇总结果。
* **🖊️ 画笔单击画点**: 钢笔/荧光笔支持单击生成圆点标注，此前单点笔迹会被直接丢弃。

### 🐞 修复与优化 (Bug Fixes & Refinements)

* **笔刷撤销/重做彻底重构**: 修复"绘制新笔迹后无法撤销""笔画橡皮擦重做会重复添加""精细擦除无法重做"等问题，改为标准 undo/redo 双栈模型，并补上此前未绑定事件的重做按钮。
* **切换日记自动保存标注**: 未点"保存标注"就切换日记导致标注丢失的问题，现在切换前自动保存。
* **文件拖拽上传目录错误**: 在子文件夹内拖拽上传会错误传到根目录，现按当前所在文件夹上传。
* **highlight.js CDN 修复**: 原 jsdelivr npm 路径返回 404 导致代码块语法高亮失效，改为 cdnjs 正确资源并更新 SRI 校验。
* **favicon 404**: 内置 SVG 品牌图标，消除浏览器 favicon 404 报错。
* **移动端画笔防滚动**: 激活画笔工具时标注层启用 `touch-action: none`，绘制过程不再触发页面滚动/缩放。

---

## 🚀 [v1.2.0] - 2026-08-01 (重磅更新)

### ✨ 核心重磅新特性 (Major Features)

* **📜 日记历史版本追踪与 Line Diff 可视化恢复 (Version History & Revert)**
  * **版本快照捕获**: 保存或修改日记时，若标题或正文发生改动，后台自动写入一条 `diary_versions` 快照记录。
  * **逐行 Line Diff 可视化对比**: 编辑器增加 `🕒 历史` 按钮，弹出历史版本对比窗口，左侧列出修改时间线，右侧以绿色背景 `+` (新增) 与红色删除线 `-` (删除) 呈现版本变动。
  * **一键安全恢复**: 支持将日记恢复至任意历史版本节点，恢复前自动对当前状态进行覆盖保护备份。

* **📱 平板 Web 端 (Tablet Web) & 移动端全方位响应式超越**
  * **平板专属 Icon Rail 侧边栏 (`769px ~ 1100px`)**: 在 iPad 和 Android 平板上，侧边栏自动收缩为 `68px` 极简图标轨，把 95% 以上视口宽度留给编辑器和视图，同时 100% 保留桌面端导航能力。
  * **移动端专属玻璃拟物底部导航栏 (`#mobile-bottom-nav`)**: 手机与小屏设备激活贴合底部导航栏，直达【日记】、【消息】、【写日记】、【文件】与【设置】5 大频道，并完美兼容 `env(safe-area-inset-bottom)` 安全边距。
  * **侧边栏抽屉遮罩 (`#sidebar-overlay`)**: 移动端唤起侧边栏时自动叠加透明磨砂遮罩，点击遮罩平滑收起菜单。

* **⚡ Command Palette 全局快捷指令面板 (`Ctrl+K` / `⌘K`)**
  * **苹果液态玻璃快捷指令**: 随时按 `Ctrl+K` 唤起全能搜索面板，支持全局搜索日记、文件、好友，一键跳转页面，以及快速切换 16 种主题调色板。
  * **触控召唤按钮 (`#btn-touch-cmd`)**: 侧边栏增加触控触发按钮，方便未外接键盘的平板/手机用户单手轻触唤起面板。

* **📝 编辑器智能防丢失自动保存 (`Auto Save`) & `Ctrl+S`**
  * **2.5s 静默保存**: 停止打字 2.5 秒后后台自动触发草稿保存，防止意外关闭标签页导致字迹丢失。
  * **快捷键保存**: 随时在编辑器中按下 `Ctrl+S` / `Cmd+S` 即可触发保存。

* **🖼️ "我的文件" 全能在线预览器 (Universal File Previewer)**
  * **全类型文件线上预览**: 点击文件直接弹出模态框预览，支持图片放大、PDF 嵌入阅读、文本代码语法高亮与音视频查看，统一提供一键复制外链与下载原文件。

* **📂 全视口 Drag & Drop 拖拽上传与发送**
  * **我的文件拖拽上传**: 将电脑文件直接拖入文件库视图，高亮蒙层 (`#files-dropzone`) 激活，放开后自动批量上传保存至当前文件夹。
  * **聊天视口拖拽发送**: 将图片或文件直接拖入消息对话视口，自动完成上传并直接将附件发送给好友。

---

### 🐞 修复与优化 (Bug Fixes & Refinements)

* **💬 消息聊天显示与气泡排版彻底重构**
  * **解决换行压字与重叠**: 给消息气泡外层增加 `.msg-bubble-container` 对齐节点，配合 `.msg-bubble-row.mine` 与 `.theirs` 设置 `justify-content`，彻底消除了旧版气泡变窄、换行压字重叠的缺陷。
  * **设计细节**: 优化气泡最大宽度 (`65%`)、行高 (`1.55`)、时间戳 `tnum` 排版以及底栏输入框交互。

* **🎨 全局设计系统与物理漫反射阴影升级**
  * **物理漫反射阴影**: 升级 `:root` 定义，引入多阶漫反射阴影 (`--shadow-sm`, `--shadow-md`, `--shadow-lg`)。
  * **弹簧过冲动效**: 引入苹果 UI 经典的 `--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1)`，赋予日记卡片 (`.diary-card`) 悬浮浮起 `3px` 弹簧拉伸与主题发光动效。
  * **触控防误触**: 移动端与平板端所有交互按钮有效响应区域提升至 `40px` 以上。

---

### 🗄️ 数据库与 API 扩展 (Database & Backend)

* SQLite 数据库新增 `diary_versions` 数据表与索引。
* `files` 表追加 `thumbnail_url` 字段支持图片缩略图外链。
* `letters` 表追加 `file_id` 外键字段，`POST /api/letters` 与收发件箱 API 支持关联发送与展示文件附件。
